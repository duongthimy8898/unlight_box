# ToggleLiveHelper

A minimal TypeScript tool to fetch the match list from the HoiQuanTV internal fixtures API.

## Usage

- `npm install`
- `npm run get-fixtures`

The tool fetches the fixture list and refreshes it every 30 seconds.

Optional:

- `npm run build`
